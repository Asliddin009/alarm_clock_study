name: alearn
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State management
  bloc: ^9.2.1
  flutter_bloc: ^9.1.1
  hydrated_bloc: ^11.0.0
  equatable: ^2.1.0

  # Alarms & notifications
  alarm: ^5.5.0
  awesome_notifications: ^0.12.1
  flutter_ringtone_player: ^4.0.0+4
  permission_handler: ^12.0.3

  # Storage
  shared_preferences: ^2.5.5

  # Media
  audioplayers: ^6.8.1
  lottie: ^3.5.1

  # UI
  numberpicker: ^2.1.2
  cupertino_icons: ^1.0.9

  # Utils
  intl: ^0.20.2
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/music/mozart.mp3
    - assets/music/nokia.mp3
    - assets/music/one_piece.mp3
    - assets/music/star_wars.mp3
    - assets/music/marimba.mp3
    - assets/json/categories.json
    - assets/images/logo.jpg
    - assets/lottie/splash_pulse.json
    - assets/lottie/onboarding_alarm.json
    - assets/lottie/onboarding_words.json
    - assets/lottie/onboarding_voice.json
